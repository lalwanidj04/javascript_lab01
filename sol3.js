console.log(2^4);
//The output is 6 because in javascript '^' is XOR operator, so it is considering it as (binary 2) XOR (binary 4) so the answer will be 6,
//To take power we need to use ** and not ^
console.log(2**4);